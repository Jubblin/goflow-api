## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] `go build -buildvcs=false ./...` passes locally
- [ ] `go vet ./...` passes locally
- [ ] README updated (if API behavior, flags, or deployment changed)
- [ ] `internal/api/openapi.json` updated (if API endpoints or parameters changed)

## Test plan

<!-- How did you verify this? e.g. curl /health, /api/v1/flows?proto=TCP -->
